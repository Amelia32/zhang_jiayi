#include <iostream>
using namespace std;
int main()
{
    double a,b,c;
    cin>>a;
    cin>>b;
    cin>>c;
    if(a,b,c>0) 
    if(((a*a+b*b)==c*c)||((c*c+b*b)==a*a)||((a*a+c*c)==b*b))
{
cout<<"这三个值可以表示一个三角形的三条边";
}
else
{
cout<<"这三个值不可以表示一个三角形的三条边";
}
}
